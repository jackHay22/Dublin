(ns dublin.map.entity.behavior
  (:require [dublin.config :as config]
            [dublin.map.environmentutils :as utils])
  (:gen-class))

(defn load-behavior-configuration
  [])

(defn update-entity-behaviors
  [entity-state])
